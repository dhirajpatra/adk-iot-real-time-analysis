# Dockerfile.mcp - For the MCP Server (Python Version)
# Assumes the MCP server is a Python application with a requirements.txt

FROM python:3.11-slim

WORKDIR /app

# Copy requirements.txt first to leverage Docker cache
# Assumes mcp_server/requirements.txt exists
COPY requirements.txt ./

RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code
COPY . .

# Expose the port the MCP server listens on
EXPOSE 4000

# Command to run the Python MCP server application
# Assumes your main server file is 'server.py' and it uses a framework like Flask or FastAPI
CMD ["python", "server.py"]