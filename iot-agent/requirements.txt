# IoT Agent Requirements - Based on Dhiraj Patra's Tutorial & Multi-Agent ADK POC

# Core web framework for API endpoints
fastapi==0.111.0
uvicorn==0.30.1

# HTTP client for Ollama communication and external APIs
httpx==0.27.0
requests==2.32.3

# MQTT for IoT communication (Crucial for sensor data)
paho-mqtt==2.1.0  # Updated to the latest stable version

# Data processing and validation (essential for API inputs/outputs)
pydantic==2.7.4
pydantic-settings==2.2.1

# Environment management
python-dotenv==1.0.1

# Date/time handling
python-dateutil==2.9.0.post0
pendulum==2.1.2 # Optional: More user-friendly datetime manipulation

# WebSocket for real-time dashboard updates (e.g., streaming sensor data)
websockets==12.0

# JSON handling for fast serialization/deserialization
orjson==3.10.3

# Asynchronous programming utilities
asyncio # Built-in Python module

# Database ORM/toolkit for interacting with PostgreSQL (if agents store data)
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9

# Other potential utilities
tenacity==8.2.3 # For retrying failed operations (e.g., API calls, DB connections)