# adk_ollama_tool/requirements.txt
fastapi
uvicorn[standard]  # Uvicorn with standard extras for better performance (e.g., uvloop, httptools)
httpx
google-adk         # Keep if your project uses a Google ADK library
litellm          # Keep if your project uses LiteLLM for broader LLM integration